# Sound assets

The app uses a system sound plus speech by default. Use the Settings panel to
choose a custom audio file for any reminder. The selected file is copied into:

`~/Library/Application Support/GlassReminders/Assets`
