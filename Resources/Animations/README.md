# Animation assets

The app includes built-in procedural water and eye-drop animations so it works
without binary assets in the repository.

Use the Settings panel to choose a custom transparent `.gif`, `.png`, `.apng`,
or any macOS-readable image. The selected file is copied into:

`~/Library/Application Support/GlassReminders/Assets`
